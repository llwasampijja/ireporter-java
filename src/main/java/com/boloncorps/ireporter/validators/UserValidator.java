package com.boloncorps.ireporter.validators;

import com.boloncorps.ireporter.models.UserReporter;

import java.util.ArrayList;
import java.util.List;

public class UserValidator {
    public String checkUsername(ArrayList<UserReporter> usersList, String username){
        for(int iterator = 0; iterator <= usersList.size(); iterator++){
//            if(usersList[iterator]["username"] == username)
        }
        return username;
    }
}
